# 《杯喜》第1集道具修稿循环

2026-09-07，源码修复完成，未发布。

## 原因与当前状态

项目 `series-31aaaf21-25f4-45fe-9996-c47010ab698e`，失败任务 `job-eff324a1-a526-428f-986f-0ef5c398f2a9`。

失败草稿 `series-drafts/c29aa8f83cc330eb5a3e649d44547cccde6d5b6182260b199daf022888b0b9a1.txt` 的第9、10镜都标了 `objectIds: ["o1"]`（白玉杯），但画面/动作只有闭眼、沉默、注视、坐下与表情反应，并没有对应道具短语。道具修稿尝试 ground 时给出了原字段中不存在的 mention，校验正确拒绝，但后续仍用相同的命名替换任务重试，最终三轮耗尽。

未把“全剧围绕白玉杯”解释为每个反应镜头都必须拍到杯子。原文明确出现的杯子或自然简称仍需保留，不因名称没有逐字匹配就自动删绑定。

本次检查时，用户后续重试任务 `job-205beea1-bcf6-4629-9387-08b8b01248d1` 已于北京时间9月6日23:47完成；当前第一集剧本的第9、10镜已明确包含白玉杯，生产任务正在生成视频。本轮保留这份成功稿，不恢复旧失败稿，也不重复购买视频。

## 修复

- 道具定点修稿失败后，切换为原文证据复核：逐项返回 visual/action 中确实存在、指向目标道具的短语；没有道具描写时返回空 mentions。
- 程序校验字段、镜号、道具ID及逐字引文。有效引文转换为原有定点替换，空清单转换为移除多挂的ID；杜撰引文、未知目标、漏项或非法结构继续拒绝，不猜测删道具。
- 恢复策略存入 generation cache sidecar，应用重启/用户重试后沿用证据复核，不退回相同的命名替换请求。全程仍受原有三轮上限约束。
- 不修改对白、动作、镜长、其他镜头或已经保存的分集；原有供应商拒绝与补镜次数限制保持。

## 验证

- 连续剧回归129/129通过，覆盖错误mention后的恢复、断点恢复、真实泛称道具保留、原稿完整性和非法证据拒绝。
- Companion生产构建通过；TypeScript及git diff --check通过。
- 对真实旧失败稿做隔离模型验证，沿用失败任务保存的 `gpt-4o` / provider `auto`，不切换模型。只发出一次真实文本请求，进入证据复核后16镜剧本完整通过。断言草稿唯一变化为第9、10镜 objectIds 清空；全部visual/action/dialogue/seconds等原字段不变。
- 证据：`outputs/cup-script-repair-20260907/receipt.json`、`reply.json`、`request.txt`、`repaired-draft.json`、`verify.mjs`。这些是隔离验证产物，未写回生产草稿或数据库。

当前运行0.1.199没有自动获得此源码修复；没有重启服务、发布或干扰正在运行的《杯喜》视频任务。
