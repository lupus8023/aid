# 更新日志

## 0.1.56 - 2026-08-24

### 修复

- 精确台词生成现在按剧本中不同 speaker ID 的数量选择单人或多人条件节点，不再按上传了多少份音色参考文件判断。
- 同一角色重复的音色参考以及没有在当前片段开口的角色参考，会在进入 H3 条件节点前去重，避免单人台词被误送进 `MiniMaxH3JointDialogueConditioningT8` 后遭到拒绝。

### 验证

- 新增“两份遗留音色参考、同一 `S2` 角色连续两句台词”的回归用例；编译后的工作流只有一个音色档案和单人条件节点，不含 Joint Dialogue 节点。
- 已确认上一段第 2 片段在生产 ComfyUI 实例成功完成，并保留原始的 `S2 · Tide Officer` 绑定。

## 0.1.55 - 2026-08-24

### 修复

- 首尾帧续接片段同时使用已校验的精确台词驱动音轨时，MiniMax H3 现在会选择 `Hybrid` 条件模式，不再以不兼容的 `Ref2VA` 任务进入执行并报错。
- H3 音色档案会在 Story 提示词、Fish Audio 音色参考和精确台词条件节点之间保留剧本中的 speaker ID。只有 `Subject 2` 开口时不会再被重新编号成 `S1`，避免作为静默听者的人鱼公主继承 Tide Officer 的男声和口型。

### 验证

- 已对照最近项目真实的 ComfyUI 提交历史确认两项错误，包括提示词中的 `Subject 2 / S2` 与音色档案中 `S1` 的错位。
- H3 音频、提示词结构、视频分段测试，以及生产网站与 Companion 构建均通过。

## 0.1.54 - 2026-08-24

### 新增

- 将官方 ComfyUI Z-Image-Turbo BF16 文生图工作流接入为全局图片服务商，Story 九宫格、单张分镜、角色草稿、定妆图和场景参考图均可选择。
- 图片任务使用独立的 `comfyui-image:` 标识，可在刷新后通过本机 Companion 恢复，并按环境返回持久 Cloudinary 地址或无损本地 PNG 数据。
- 全局模型选择器明确显示 Z-Image-Turbo 的真实能力边界：支持文生图；需要参考图编辑时继续使用 APIMart，不会静默丢弃编辑输入。

### 优化

- Z-Image 不能读取视觉参考时，会保留角色与物体的文字设定；九宫格提示词使用更长预算，避免后几格因 APIMart 的短提示词限制被截断。
- Companion 跨域白名单覆盖网站所需的全部静态图片生成与状态查询路由。

### 验证

- 已在 49GB 4090D 的仙宫云 ComfyUI 安装官方 BF16 扩散模型、Qwen 3 4B 文本编码器和 Flux VAE；三个文件尺寸与官方一致，并在队列安全重启后被 ComfyUI 正确识别。
- 使用 8 步工作流实际生成并下载一张 1344×768 验证图；生产网站构建、Companion 构建、图片模型、角色设计、九宫格恢复与 Companion 跨域测试均通过。

## 0.1.53 - 2026-08-24

### 优化

- Story 在全片因果骨架之后一次性撰写完整台词稿，并在逐镜执行前锁定说话者、逐字台词、语义证据、潜台词和听者变化，避免关键对白退化成互不连贯的短口号。
- 全片明确保存七个结构节点、观众认知变化和镜头剪辑交棒，并贯穿剧本、分镜、H3 提示词、片段缓存签名和交付审计。
- 详细剧本改为逐镜补充表演、状态和声音执行，但每镜事件与台词仍由全片稿统一授权；模型返回不完整数组时不会再拖垮整部长片，也不会割裂故事。
- H3 蒙太奇提示词保留合并片段内每个分镜的秒数、具体动作、运镜、逐字台词和有动机的物理转场；兼容镜头仍会在 15 秒限制内自动合并。

### 修复

- APIMart GPT-4o 输出预算限制在服务商 16K 上限内，18–81 镜长片不会再因最后一批只返回一镜而整体失败。
- 英文项目遇到执行模型翻译了已被全局骨架覆盖的临时字段时会确定性修复，不再丢弃已经正确锁定的英文剧情与台词。
- Story 交付门禁会在生成视频前定位缺失结构节点、剪辑交棒、缩短的锁定台词、丢失语义和错序对话。

### 验证

- 最近项目完成一次真实 18 镜英文改编：7 个结构节点、23/23 条锁定台词、18 张 1:1 分镜，端到端故事交付审计为 0 错误。
- 自动剪辑得到 14 个 H3 片段，其中 4 个为多分镜蒙太奇片段；全部不超过 15 秒。Story、交付、分段、H3 相关测试与生产构建通过。

## 0.1.50 - 2026-08-24

### 修复

- 角色音色参考改为带版本的 Fish Audio 无语义音色探针，普通示例句中的单词不会再泄漏到 MiniMax H3 台词开头；已保存或导入的旧版有语义参考会自动失效，每个开口角色只需重新生成一次。
- Story 台词改为原生 H3 两阶段链路：先由 H3 按锁定音色生成剧本中的逐字台词，经 faster-whisper 校验并裁切，再用这条已验证的 H3 音轨驱动最终 Ref2VA 表演和口型，同时由 H3 重混环境声。
- 最终视频提示词会把已验证音轨视为权威对白，而不是宽松的音色参考；导演说明、参考音频音节、旁白和即兴词不会进入人声层，画面动作对应的环境声与拟音仍可保留。

### 验证

- 使用既有分镜实际重跑一段 13.67 秒、1:1 的 Story 样片；Whisper 只转写出 Tide Officer 的两句指定台词，没有开头参考词、动作旁白或额外人声，台词结束后的尾部仍保留非静音环境声。
- TypeScript、Story/H3/音色/分段相关回归测试和 Companion 生产构建通过，H3 缓存契约升级为 v13。

## 0.1.49 - 2026-08-24

### 优化

- Story 会先完成全片因果骨架，再按较小批次撰写详细剧本；每镜保留原始镜头映射、戏剧变化、观众信息、对白功能、动作、运镜、声音和物理衔接，长篇改编不再退化成彼此孤立的漂亮画面。
- 分阶段编剧提高了结构化输出预算，按对白密度切分批次，并增加有上限的自动纠错重试；18–81 镜项目不会再因为外层 JSON 被截断而误报“返回 0 镜”。
- 剧本发现的每个开口配角都会进入有效制作角色表，拥有可复用角色卡、明确的性别/年龄/身份、确定性的 Fish Audio 音色以及 Story 内的音色操作入口；自动失效转移不会跨越男女声池。
- MiniMax H3 会收到片段内每个分镜的完整时间契约：具体动作阶段、运镜、逐字台词、环境声、因果拟音和物理转场。多分镜密集片段使用无损精简格式，保留必要信息并控制在 H3 提示词上限内。

### 修复

- 必要台词会贯穿骨架标准化、详细剧本、分镜导演和片段合并；导演说明、未绑定临时人物和重复台词会在成为可听人声前被拦截。
- 剧本自动生成的配角现在可以参与分镜生图、角色参考卡、音色参考和项目断点恢复，不再出现空白角色卡或错误继承主角声音。
- 进入分镜导演前新增故事交付审计，缺失的因果、角色绑定或必要台词会在剧本阶段明确定位；内容安全拒绝会自动改写成非血腥的剧情表达，同时保持用户锁定事实和台词不变。
- 开发环境的 Companion 允许其他本机端口访问 Story，生产环境的来源限制保持不变。

### 验证

- 新增故事交付、分阶段剧本、群像音色与 H3 提示词预算回归；打标签前通过 TypeScript、生产构建和 Companion 发布测试矩阵。

## 0.1.40 - 2026-08-23

### 优化

- “改编剧本”不再强制所有新增台词都写成短句，而是先规划完整的场次对白单元；提问、拒绝、决定、承诺与回收会跨镜共享上下文，并明确禁止脱离指代的孤立口号。
- 剧本与导演契约新增对白单元、必要性、听者变化和蒙太奇句法；承担关键信息的必要台词不能在详细剧本阶段静默降级为纯视觉镜头。
- H3 v6 继续保留合并片段中每镜的实时动作和物理转场，只把可见的听者表演传给视频模型，抽象剧本说明仍与视听通道隔离。

### 修复

- 每个有台词的角色现在会在全片锁定同一个、符合角色资料的 Fish Audio 音色；年轻女性美人鱼会自动匹配年轻女声，用户手选音色保持最高优先级，旧项目在加载或导入时自动迁移。
- Fish Audio 台词和角色音色参考接口会拒绝缺少 `voiceId` 的请求，不再静默使用可能改变性别或角色身份的默认声音。
- 每条权威台词都会保存角色音色并纳入 H3 缓存签名，旧版错误男声生成的片段和锁定音轨不会被重新复用。

### 验证

- 新增自动音色选角、用户音色保留与缺失音色阻断测试；Story、H3、分段、无人托管、恢复、导出及 Companion 路由测试和生产构建均通过。

## 0.1.39 - 2026-08-23

### 优化

- 剧本原文中的明确台词会作为规划、分镜导演和合并 H3 片段的权威输入；剧情确有对答需要时，单镜最多保留四条有序台词，旁白标签和表演说明不会再被当成说话者。
- H3 片段时长会预留与精确音轨编译器一致的开口前、收尾后留白，并向上取整所需秒数，避免合并台词被挤进过短片段。

### 修复

- 九宫格、拆分分镜、剧本更新、视频缓存完成、项目导入和暂停状态都会立即持久化，不再等待下一次定时保存。
- 当前项目改用带版本的权威存储键；旧部署留下的标签页仍可更新兼容数据，但不能再覆盖最新分组、视频任务号和已完成时间线。
- “一键成片”暂停后立即继续时，会先等待旧编排锁退出，再从第一个未完成阶段启动接替任务，不再停在等待状态。
- 导入项目时会同步刷新内存中的故事计划并立即落盘，断点续跑不会引用导入前的旧计划。

### 验证

- 以最近的 1:1 Story 项目完整重跑：27 张新分镜、16 个合并 MiniMax H3 片段、本地缓存及 Companion FFmpeg 合并全部成功，得到 158.21 秒、960×960 成片。
- Whisper 复核覆盖全部剧本权威台词，没有额外中文台词、表演指令或不明人声；Story 分阶段、片段编排、H3 提示词、无人托管和项目隔离测试以及生产构建均通过。

## 0.1.32 - 2026-08-23

### 优化

- Story 的执行编剧与分镜导演阶段融合了 `video-shotcraft` 中适合本产品的导演原则：每镜一个主动作弧、加速进入明确触点、结果短回落、相机服务情绪，以及每个接缝只使用一种物理交棒语法。
- H3 片段会为合并后的每个分镜保留完整的秒数、动作、运镜、台词和物理衔接，并延续运动矢量、速度与银幕方向，不再依赖叠化或装饰性漂移。

### 修复

- 不再把剧本中的前因、压力、选择和观众理解等抽象解释重复塞进 H3 视听描述，避免 Ref2VA 把本应静默的导演说明误念成额外旁白。
- H3 提示词会声明本段唯一允许的可理解人声数量；只有时间表中带标签的精确台词可以发声，旁白、即兴词、歌唱、参考音频原话和背景可辨识人声全部禁止。
- H3 提示词契约升级后会自动让旧引擎生成的片段缓存失效，断点续跑不会继续复用带旧台词或旧动作规则的视频。
- 剧本与多分镜 H3 生成统一要求 Companion v0.1.32，确保线上前台和本地生成接口使用同一套导演与声音规则。

### 测试

- 新增静默解释文本、精确 H3 人声事件数、ShotCraft 动作/衔接规则和提示词契约缓存失效测试；109 项测试、TypeScript 校验和生产构建全部通过。

## 0.1.31 - 2026-08-23

### 修复

- 27–81 镜长剧本现在要求 Companion v0.1.31，避免本地长请求被旧版剧本接口接管并静默丢失核心悬念、观众信息、对白功能和蒙太奇字段。
- 全片骨架的输出预算会按新版逐镜叙事字段扩容，避免 27 镜规划在 JSON 结尾被截断后误报“返回 0 镜”。
- 故事骨架、逐镜剧本和导演批次改为三次带短退避的尝试，APIMart 瞬时 TLS 重置或超时不会立即耗尽全部纠错机会。
- Companion 的公共 DNS HTTPS 通道若在收到响应前失败，APIMart 剧本请求会自动回退系统网络栈；既保留污染 DNS 电脑的兜底，也不会破坏本来依赖系统代理的电脑。
- 证书主机名不匹配会被识别为公共 DNS 污染；首次出现这类错误或响应前重置后，本次 Companion 进程的后续编剧/导演批次会持续使用已验证的系统网络链路。
- 导演 description 中重复出现的精确台词会在校验与合并前被确定性移除，不再依赖模型反复改写，也不会把对白复制进可能被 H3 念出的视觉说明。
- 台词剥离兼容句号、破折号和引号差异（例如 `Again.` 与 `“Again—”`），并清除孤立西里尔残词；整段输出语言错误仍会被拒绝。
- 导演批次会拦截中英文串写、异常西里尔残词，以及在画面描述中重复精确台词的输出；不合格批次会在进入生图与 H3 前自动纠错重试。
- 台词归属以精确的画面出场角色列表为权威；英文 action 可以自然写成 “the mermaid princess”，音色绑定仍保留上传角色名“人鱼公主”，不会因叙述别名误判失败。
- 总纲中任何非纯视觉对白功能都必须先绑定已上传角色；没有有效音色归属的规划会改为可见剧情信息，不再在“缺台词”和“临时角色禁止发声”之间循环失败。
- 模型若在合法台词旁额外添加临时人物回应，剧本会保留已绑定角色台词并只删除无归属内容，不再让整批失败，也不会合成陌生音色。
- 仅由 AI 规划、没有用户锁定原文的对白若无法生成合法绑定，可回退为视觉回收；用户锁定台词仍强制保留，兼顾不中断流程与不擅自编造替代台词。
- “一键成片”新增浏览器级、按项目隔离的托管锁；同时打开或刷新多个 Story 标签页时，不会再重复提交同一付费生图/视频片段，只有原托管页关闭或崩溃后候补页才会自动接管。
- 某个标签页正在托管一键成片时，其他旧标签页会暂停周期性自动保存，避免用旧快照覆盖最新分段方案、任务号和缓存状态。

### 测试

- 新增导演语言、精确台词隔离和跨标签页托管锁回归测试，并以最近的 27 镜项目执行完整“一键成片”验收。

## 0.1.30 - 2026-08-23

### 优化

- Story 骨架新增全片核心观众问题、观看承诺、对白弧线与蒙太奇策略，并把它们传入每个场次；每镜必须带来明确的观众信息增量，同时留下推动下一镜的问题。
- 剧本 beat 明确区分可见动作、戏剧变化、信息增量、对白功能和蒙太奇语义；问题、揭示、选择、承诺与回收会跨镜规划，不再生成孤立口号或一串漂亮但并列的事件。
- 有叙事必要的对白不再被“每镜最多一句”的旧规则一律压缩；在动作与 15 秒预算允许时，一镜可以容纳两条严格按顺序、绑定音色的“发起→回应”或“揭示→决定”台词。
- 导演与 MiniMax H3 提示词会逐镜编译剧本的触发、压力、可见选择、后果和观众应理解的信息；因果、平行与对照蒙太奇会驱动物理镜头衔接，不使用淡入淡出，也不补画外解释。

### 修复

- H3 片段不再让静态分镜生图 prompt 覆盖编剧锁定的动态 action。
- 同一分镜的多条台词会按顺序安排开口留白、回应间隔和说后反应；只有精确台词能进入 H3 的对白标签。
- 已规划对白功能但缺少说话者、可朗读原文、叙事功能或画面内角色时，剧本阶段会判定无效并结构化纠错重试，不再到下游静默变成无台词或断裂台词。
- H3 缓存签名新增因果故事、信息增量、对白功能与蒙太奇字段；修改叙事后不会复用旧片段。

### 测试

- 新增故事/观众契约、单镜双台词时间表、H3 因果编译和叙事版本缓存失效回归测试；103 项测试、TypeScript 校验和生产构建全部通过。

## 0.1.29 - 2026-08-23

### 新增

- “一键成片”升级为可断点恢复的无人托管流程，会依次完成剧本/分镜、制作参考、分镜图、分组 H3 片段、本地缓存、Companion FFmpeg 合并与最终下载。
- “一键成片”新增真正的暂停/继续：暂停会保留已完成产物和已提交的供应商任务号，继续时从第一个真正未完成项开始。

### 修复

- 一键成片遇到短暂的生图、视频、Companion 或导出失败后不再静默退出；可恢复阶段按 3/8/15/30/60 秒有上限退避持续重试，直到成功或用户明确暂停。
- 已存在的分镜图 URL 成为完成状态的权威依据；即使旧项目状态仍显示等待或生成中，刷新与续跑也不会重复生图。
- 中断的付费 3×3 生图会重连同一个 APIMart 任务并重新拆分结果；部分完成但母图任务已不可恢复时，只单独补齐缺失分镜，不覆盖成功图片。
- 单张补图任务与九宫格任务会立即持久化并明确区分，避免刷新后把单张任务误当成九宫格恢复。
- 保存的 ComfyUI 视频任务号在网络超时后会继续轮询；只有供应商明确返回终止失败时才允许提交替代任务。已完成片段会跳过，本地成片导出也会自动重试。

### 测试

- 新增“产物优先”续跑、付费九宫格重连、局部单镜补图、任务类型隔离和无人托管退避时序回归测试；九宫格恢复、本地原生导出与生产构建均已通过。

## 0.1.28 - 2026-08-22

### 修复

- Story 视频片段新增确定性的“制作输入签名”，覆盖分镜图、动作、运镜、台词、时长、连续性、画幅和制作风格；修改或重新生成任一输入后，受影响片段会自动失效，不再静默复用旧视频和旧音轨。
- 视频持久缓存键除了项目与分镜身份，还会包含本次生成签名，避免同一项目早期片段把旧版旁白串入重新编辑后的成片。
- 4K 九宫格分镜的轮询上限由 4.5 分钟延长到 9 分钟，避免 APIMart 付费任务仍在正常生成时前端提前误报超时。

### 测试

- 新增制作版本缓存隔离、分镜图或台词变化后自动失效片段的回归测试；生产构建、H3 音频/提示词、本地导出、九宫格恢复、片段编排和项目隔离测试全部通过。

## 0.1.27 - 2026-08-22

### 优化

- Story 在展开分镜前会为每句生成台词绑定明确的已上传角色；临时人物或无名人物的内容只保留为可见表演，不再错误转交给主角说出。
- MiniMax H3 片段提示词改用官方六段式 Ref2VA 契约及官方镜头/时间戳语法；同一片段里的每个分镜仍完整保留动作、运镜、秒数、镜头衔接和授权台词，同时移除可能被 H3 误读成语音的自定义伪控制字段。
- 仙宫云 MiniMax H3 T8 节点已由 v1.3.2 升级到 v1.43.0；AID 三套 H3 工作流的核心节点均保持兼容，旧版节点也已移出扫描目录并保留备份。

### 修复

- 如果生成台词的说话者没有出现在当前分镜中，系统会自动隔离该台词：前台显示具体原因，后端不会把它写入 `<d>`，也不会用它生成声音参考。
- 剧本规划会校验“必说台词”和有效角色归属；模型返回无说话者或错误人物时自动进入纠错重试，不再把归属错误传递到分镜、生图、视频和原生音频阶段。
- 视频提示词覆盖会清除旧项目残留的 `SPEECH GATE`、`DIALOGUE`、`SPOKEN_WORDS_ONLY` 和 `NON_SPOKEN_PERFORMANCE` 字段，避免导演指令再次被当成对白。

### 测试

- 新增缺席角色台词隔离、剧本说话者校验、H3 官方镜头格式和旧伪控制字段清理的回归测试。

## 0.1.26 - 2026-08-22

### 优化

- Story 自动片段编排会按每镜真实动作/对白时长、最多 4 镜、最多 3 条对白和 H3 15 秒上限组合连续分镜；换地点、换 sequence 或模型写出的淡入淡出不再机械地把每镜拆成独立片段。
- 合并后的每个分镜仍保留独立时段、动作、运镜、对白与因果衔接，只改变 H3 的生成批次。

### 修复

- 手动拆分、合并和边界调整现在保存为项目级“导演编排”，操作后立即持久化；刷新页面、重新打开项目、无限画布和“一键成片”都会沿用同一分组，不再恢复成自动单镜分段。
- H3 可见动作、分镜描述和用户覆盖提示词会清除夹带的引号台词及“喘息着喊”等发声指令，真正对白只允许在权威 `<d>` 标签内出现一次。
- 配置 Fish Audio 时，Story 会为当前片段生成只包含准确台词的角色声音参考，并按台词签名缓存；修改台词后会自动失效重建，不再把通用音色样本中的示例句带入成片。
- Companion 会将不足 2 秒的准确台词参考补静音至 H3 最低长度，同时继续限制全部参考音频不超过 14.7 秒，避免短台词生成失败或回退到错误声音。

### 测试

- 新增自动跨场景合并、15 秒预算、手动导演编排序列化恢复、视觉通道台词清理、准确短台词参考补齐等回归测试；全套测试和生产构建通过。

## 0.1.25 - 2026-08-22

### 优化

- Story 会把编剧阶段每个 beat 的权威可见动作完整带入分镜，不再要求最终视频提示词从静态生图描述中猜测动作。
- 合并到同一个 MiniMax H3 片段的每个分镜都会获得独立的精确时段、动作节奏与落点、运镜、台词状态、可见动作声和有动机的电影化衔接。合并只改变生成批次，不再压缩任何分镜内容。
- H3 动作时间表会直接进入运动，在末段反应窗口前完成关键接触或转向，并以真实物理速度保留次级运动，避免把一个动作拉长成类似慢镜头。
- 首尾帧连续模式把尾帧作为动作落点：主体动作在最后 16% 之前完成，不再用整段时间在两个静态姿势之间均匀慢插值。

### 修复

- “先短暂停顿，再以坚定语气说”等模型生成的表演说明会在保存或重新生成前从台词中剔除；若包含引号，只保留引号内真正说出口的话。
- 情绪、停顿和说话方式会转换为不可朗读的 H3 结构化控制码，只有权威 `<d>` 标签内的文字允许发声。
- 分镜参考图只锁定身份、服装、地点与光线，不再锁死姿态、调度与机位，使每个分镜可以完整执行动作和运镜。
- 四分镜 H3 完整结构保持在供应商 7000 字符限制以内，同时不丢失动作、运镜、台词状态或镜头衔接。

### 测试

- 新增合并片段逐镜时间表、完整动作/运镜/台词契约、电影化衔接、首尾帧节奏、表演指令过滤和 H3 提示词预算回归测试。

## 0.1.24 - 2026-08-21

### 新增

- 全局生图模型新增 APIMart Nano Banana 2（`gemini-3.1-flash-image-preview`）和 Grok Imagine 2.0 官方版（`grok-imagine-image-2.0`），Story 九宫格与单镜、人物/场景素材、角色设计和图生图均可使用。
- 独立角色设计工作台保存的角色会自动进入 Story“历史角色”，并携带完整角色卡、身份、年龄、性格、主题、服装和制作风格供后续项目复用。

### 优化

- 九种制作风格分别获得明确的媒介/材质、可解释光源、镜头/景深和色彩契约，不再只依赖泛化的风格标签。
- “自然电影”进一步强调相机或手机直拍的皮肤毛孔、发丝、织物、有限动态范围、真实光学景深和克制镜头特征。
- 九宫格提示词保留结构换行，并优先在单词边界裁剪逐格描述，同时完整保留九个画面身份与精确人物约束。

### 修复

- 统一 APIMart 生图适配层会正确处理 Grok 的 `aspect_ratio`、响应版本头、2K/三张参考图上限和对象式任务 ID，同时保留 Nano Banana 2 的 4K 与十四张参考图能力。
- Story 会同步限制参考图和提示词中的编号，避免供应商参考图数量较少时出现 `Reference image N` 缺失或错位。
- 图生图与角色设计会根据当前模型调整参考图数量，不再提交模型不支持的请求体。

### 测试

- 新增全局模型选择、供应商专用请求体与任务 ID、Story 角色库迁移、不同风格成像契约和九宫格提示词结构的回归测试。

## 0.1.23 - 2026-08-21

### 新增

- 首页新增独立“角色设计”工作台：可组合文字简报与最多 4 张参考图，探索 4 款或 9 款可选形象，并把锁定方案扩展成可用于生产的 4:3 完整角色卡。
- 完整角色卡会把身份、年龄、性格关键词和核心主题带入转面、轮廓、表情、姿态、材质、动作细节与连续性色板模块。
- 角色草稿同时支持 2×2 与 3×3 高清拆分、下载、重新生成和本地角色库存储。

### 修复

- MiniMax H3 台词提示词改为官方格式：说话者身份、动作和表演方式放在 `<d>` 外，`<d>` 内只保留真实语言标记与逐字对白。
- “无其他角色在场”“其他角色保持沉默”等导演指令不再作为 speech 字段要求、不再重复写进声音总览，旧项目中的同类脏台词也不会进入 H3 对白。
- `overall_soundscape` 只保留环境声、可见动作声和明确要求的非语言背景人声，不再混入台词控制说明。

### 测试

- 新增 H3 导演指令泄漏、逐字台词位置、4/9 款角色草稿、角色卡元数据以及 2×2/3×3 拆分回归测试。

## 0.1.22 - 2026-08-21

### 修复

- 单镜头导演批次不再把合法的单元素 JSON 数组误截成对象，继而错误提示“返回 0 镜，要求 1 镜”。
- 分镜导演会先兼容直接镜头对象以及常见的 `shots`、`storyboards`、`items`、`data` 供应商包装，再严格校验目标镜头数量。
- 结构化响应提取会保留真正的 JSON 外层结构，兼容代码块和前后说明，并正确处理字符串内部的括号与大括号。

### 测试

- 新增单元素数组、单镜头对象、嵌套 JSON 标点和常见供应商包装格式的回归测试。

## 0.1.21 - 2026-08-20

### 优化

- Story 现在会先锁定精简的全片故事骨架与精确镜头地图，再按场次以最多 9 镜一批展开详细剧本，最后使用相同批次边界生成导演分镜提示词。
- 每个批次都会获得全片故事脊柱、上一批交接状态和后续两镜路线。同一场次严格延续人物调度与光线；新场次可以明确重建时间、地点、构图和光源，同时保留人物与剧情状态。
- 原“AI 扩写”升级为“改编剧本”：根据当前选择的 9–81 镜制作规格和预计片长，把原始内容预先改编为数量精确、编号连续且前后因果相连的剧情节拍。

### 修复

- 长片不再要求 DMX 在一次响应中同时返回最多 81 镜的故事结构、详细剧本、声音规划和摄影提示词；每个阶段现在具有独立的输出字数与超时预算。
- DMX 请求超时后不再通过另一个 OpenAI 接口重复发送同一份过大的内容，避免连续等待两个 4 分钟超时。
- JSON 格式或镜头数量不正确时会在对应批次内自动纠正重试，并报告准确镜头范围，不再无法定位地丢弃整份剧本。

### 测试

- 新增分阶段生成回归测试，覆盖精确镜头配额、连续编号、场次安全的 9 镜分批、提示词阶段隔离与导演批次一致性。

## 0.1.20 - 2026-08-20

### 优化

- Story 分镜导演不再堆叠泛化的“电影感”词汇，改为明确相机距离、镜头透视、构图与遮挡、焦点平面与景深、可解释光源、有限曝光、材质响应和符合成像系统的细微镜头特征。
- 项目制作风格现在会贯穿导演与静帧生图链路；连续镜头共享相机/渲染家族和色彩响应，同时保留每镜独立的光影、景深与透视差异。
- 九宫格提示词改为“精简项目成像基线 + 每格摄影差异”，总预算限制为 3500 字符；裁剪重复连续性说明前，优先完整保留九个场景身份、精确人物数量与核心成像物理。

### 修复

- 半透明强调色选中卡片不再误用实心按钮的深色文字与实心 Hover；Story 画幅选中项会正确显示主题色文字和图标。

### 测试

- 新增静帧摄影物理、九宫格精简提示词、供应商字数预算与 Panel 1–9 完整保留的回归测试。

## 0.1.19 - 2026-08-19

### 新增

- Story 导出会先把所有 H3 成功片段逐个保存到 Companion 的持久目录，再启动 FFmpeg；刷新浏览器不再丢失成片源文件。
- 本机导出任务会持久记录片段清单、中间文件、进度和最终成片，Companion 重启后可以从未完成步骤继续。

### 修复

- Story 导出不再卡死在浏览器 FFmpeg WASM 的 5%；Companion v0.1.19 改用随应用打包的原生 FFmpeg 合并。
- 片段下载、逐段标准化和最终拼接分别最多自动重试 3 次；SHA-256 校验成功的片段及有效中间文件会直接复用。
- 合并前统一分辨率、24fps、像素格式与音频规格；没有音轨的片段会补静音轨，降低长片拼接失败概率。
- 未完成任务会保留浏览器恢复标记，再次进入同一项目的导出页后自动续跑；最终文件只在本机合并完成后传回，不再进入浏览器 WASM 内存。
- 成片交付改用浏览器 Blob 下载，避免 HTTPS 页面直接跳转 localhost 被 Chrome 拦截。
- 关闭本机 Companion 时使用的浏览器备用导出增加加载超时和明确提示，不再永久停在“准备 FFmpeg”。

## 0.1.18 - 2026-08-19

### 修复

- DMX 的 GPT-5 系列剧本模型现在优先使用官方 Responses API，仅在需要时回退到 Chat Completions。
- 剧本接口兼容标准 Chat Completions、内容数组、结构化 parsed 输出、Responses API 与常见网关包装，不再只识别 `choices[0].message.content`。
- DMX 空响应会区分 token 截断、模型拒绝、传输失败与返回结构，并且不会把提示词、生成内容或密钥写入日志。
- 检测到旧版 Companion 时会明确要求更新 v0.1.18，不再把长剧本请求静默转到容易超时的托管接口。
- 分镜生图现在会在提交前定位高风险镜头，生成独立的非血腥生图提示词；APIMart 安全拒绝后会逐级降低伤害表现并自动重试。
- 九宫格与单张分镜都会保留原始 prompt，同时在卡片上显示诊断原因、候选镜头和自动重试次数。

## 0.1.17 - 2026-08-19

### 优化

- Story 的九种制作风格现在分别向 MiniMax H3 最终提示词写入表演、动作、剪辑和声音规则，不再主要停留在色彩预设。
- “自然电影”明确模拟真实相机或手机直拍：有限曝光宽容度、真实对焦恢复、手持惯性、滚动快门以及皮肤和织物细节；“观察纪录”则独立强化手机/随身相机的现场感。
- 每种风格的声场均约束为画面可见动作所导致的环境声和拟音，避免莫名台词与装饰性声音。

## 0.1.16 - 2026-08-19

### 新增

- Story 视频阶段升级为“片段编排”：AI 自动把连续分镜组织成 1–4 个节拍、最长 15 秒的 H3 片段，并支持点击拆分、合并和双向调整边界。
- 无限画布改为故事 → 九宫格批次 → 独立分镜 → H3 片段 → 最终时间线的结构化流程，不再把一个分镜机械对应成一个视频。
- 九宫格改为 4K 母图生成；拆分阶段保留高清母图，并对每格执行裁切、1600px 有效边限制和质量优先压缩，H3 提交前再做画幅与 1.6MB 体积预处理。

### 改进

- MiniMax H3 提示词采用官方 Base/Ref2VA 结构：统一引用定义、保留关系、逐镜时间线、稳定说话人编号、环境声和配乐字段。
- 每个 H3 片段明确建立、推进、转折和落点，镜头衔接使用可见因果、动作承接、银幕方向和物理状态，强化电影节奏与整体剧情感。
- 视觉风格、角色数量、道具状态和表演动作改为正向约束，减少会稀释主动作的重复负面关键词。
- 台词采用 H3 的 `<d>[Language]…</d>` 结构，声音参考只绑定音色与表达；无台词镜头保持非说话表演与可见动作拟音。
- 禁止画面文字的规则收敛为一次简洁的干净画面约束，不再在每个分镜重复字幕相关词汇。

## 0.1.15 - 2026-08-18

### 修复

- 剧本 API 新增“自动 / 仅 DMX / 仅 APIMart”明确选择；仅使用模式不再静默切换供应商。
- Companion 为 DMX 和 APIMart 使用独立公网 DNS，绕过代理/VPN 的 `172.19.x.x` Fake-IP 解析。
- 本地剧本接口中断时直接显示 Companion 错误，不再回退到容易超过 60 秒限制的托管函数并最终返回 HTML 网关页。
- 自动模式会同时保留 DMX 与 APIMart 的失败原因，便于判断 Key、模型或网络问题。
- 图生图参考素材改为浏览器端压缩并逐张上传，生图接口只传 URL，避免多张 Base64 原图超过网关请求体限制。
- 图生图、角色定妆和单张分镜生成统一安全解析空响应、HTML 网关页和 JSON 错误，不再显示 `Unexpected end of JSON input`。

## 0.1.14 - 2026-08-18

### 修复

- Story 连接测试不再在新电脑上下载并解析无关且体积较大的视频换人物工作流。
- 连接测试分别验证设备密钥、H3 工作流与 ComfyUI API，并在浏览器/本地网络中断时显示可执行的诊断信息。
- SSH 地址、端口或实例不可用时，连接测试会快速返回明确错误，不再长时间卡住后显示 `Load failed`。
- Companion 授权和 Story 正式连接现在统一使用公网 DNS 解析，修复代理/VPN 将仙宫云域名解析到内网假地址后“显示已授权但实际连接失败”的问题。

## 0.1.13 - 2026-08-18

### 修复

- 连续镜头改为在终止静帧前交接运动，并在合并时同步裁切接缝两侧。
- MiniMax H3 只允许剧本中已确认的台词和必要的轻量现场声；声音参考不再被当作台词内容。
- 图片与视频提示词增加逐节拍精确出场人数约束，避免人物重复或额外生成。
- 剧本规划默认采用视觉叙事，不再自行添加台词、旁白、字幕或无来源人声。
- Companion 明确采用每台电脑独立的设备密钥，并提示新电脑需要分别授权。
